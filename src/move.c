#include "move.h"
#include "board.h"

int validStringMove(char *string) {}

// validStringMove() must be called before this function.
Move stringToMove(char *string) {
    Move move;

    // File (Column) (Char)

    // Rank (Row) (Int)

    return move;
}

void movePiece(Board board, Move move) {}